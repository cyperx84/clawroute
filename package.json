{
  "name": "clawroute",
  "version": "0.0.1",
  "private": true,
  "description": "Runtime routing, policy, and budget control for OpenClaw",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx src/cli.ts",
    "route": "tsx src/cli.ts",
    "test": "node --import tsx --test test/**/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "ajv": "^8.17.1",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
